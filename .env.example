# === Telegram and model keys ===
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_KEY

# === Pinecone (DEPRECATED - replaced by SQLite vector store) ===
# PINECONE_API_KEY=YOUR_PINECONE_API_KEY
# PINECONE_ENV=YOUR_PINECONE_ENV
# PINECONE_INDEX=YOUR_PINECONE_INDEX

# === Links and identifiers ===
CORE_CONFIG_URL=https://updates.ariannamethod.me/core.json
GROUP_ID=ARIANNA-CORE
CREATOR_CHAT_ID=YOUR_TELEGRAM_USER_ID

# === Resonance identities ===
OLEG_IDS=123456789,987654321  # Comma-separated list of Oleg's Telegram user IDs
ARIANNA_IDS=111111111,222222222  # Comma-separated list of other Arianna incarnation IDs

# === Core and log file paths ===
ARIANNA_CORE_PATH=./config/core.json
CHRONICLE_PATH=./config/chronicle.log
VECTOR_DB_PATH=./data/vectors.db  # SQLite vector store (replaces Pinecone)

# === Optional behavior tuning ===
IS_GROUP=True      # Use group etiquette in the system prompt
# Delays (reduced for faster responses; Oleg gets 0.5-2s automatically)
GROUP_DELAY_MIN=10      # was 60
GROUP_DELAY_MAX=45      # was 600!
PRIVATE_DELAY_MIN=3     # was 20
PRIVATE_DELAY_MAX=15    # was 180!
SKIP_SHORT_PROB=0.3     # Skip short non-question messages (feature, not bug!)
FOLLOWUP_PROB=0.2
FOLLOWUP_DELAY_MIN=900
FOLLOWUP_DELAY_MAX=7200

# === Redis and counters ===
REDIS_URL=redis://localhost:6379/0
RATE_LIMIT_MAX=5
RATE_LIMIT_INTERVAL=60
COUNTER_TTL=60
